Mindless is a low level c++ game engine in the works. 
The engine uses Data-oriented design paterns and the goal is to minimize the use of libraries(e.g c++ standard lib)
The only libraries in use are OpenGL and the win32 API.
I am currently implementing the memory manager and 3D renderer. 
